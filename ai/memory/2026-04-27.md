# 2026-04-27

## Summary

- Fixed the control-console memory records crash caused by treating a Vue ref as an array in script logic.
- Removed redundant memory config panels and stopped default-selecting the destructive clear-all option.
- Replaced external memory-record editing with the in-page CodeMirror editor so the control console stays server-safe.
- Corrected daily log selection so the UI and runtime only target today's `ai/memory/YYYY-MM-DD.md` file.
- Integrated `LONG_TASKS.md` into control / Hermes runtime memory injection and reasoning planner defaults.
- Added planner-side memory sync constraints so state-changing runs update `STATUS.md`, `TASK_QUEUE.md`, and when relevant `DECISIONS.md` and `LONG_TASKS.md` before the final answer.

## Notes

- The next required check is an end-to-end runtime or reasoning run that reaches reviewed write steps and confirms the memory files are updated after approval.
- This file exists so the current-day log record opens a real 2026-04-27 file instead of falling back to stale history.
